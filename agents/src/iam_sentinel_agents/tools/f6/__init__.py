"""F6 Shadow Guard -- shadow_guard_ingest (CloudWatch-Logs-driven, not
agent-callable) + shadow_guard_report (agent-callable via sentinel_handler)
+ shadow_guard_scp_refresh (scheduled, not agent-callable)."""
