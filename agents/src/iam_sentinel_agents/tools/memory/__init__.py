"""Memory Fabric tool layer (phase-14): `recall`/`remember` wrappers over
`iam_sentinel_adapters.memory.client.MemoryClient`, the shared
`MemoryActions` Bedrock action-group Lambda, and the semantic syncer.
"""
