IAM SENTINEL — EXECUTION STATE
==============================

This file is the durable checkpoint the assistant reads on "start" and
updates on "stop". Format is human-readable so you can inspect and edit
it directly. Fields are key: value. Lists are indented with "- ".

Contract: see docs/EXECUTION_PLAN.txt §8. The assistant mirrors the cursor
to its persistent memory (`project-execution-cursor` entry); if that memory
and this file disagree, this file wins.

------------------------------------------------------------------
CURSOR
------------------------------------------------------------------
CURRENT_PHASE: (none)
STATUS: done
BRANCH: main
STARTED_AT: 2026-07-30
LAST_STEP: frontend phase-01 (Chat Console) done on feat/frontend-chat, merged to main at 1ccf385, tagged phase/frontend-chat-done. See HISTORY for full detail. Real toolchain run: pnpm typecheck/lint/test/check:api-drift clean (14 unit tests); backend ruff/ruff format/mypy --strict/pytest clean (93 tests, 92% coverage) after a real cross-phase fix to backend/ws/fanout.py (see HISTORY, ADR 0022). Deferred: Playwright e2e (chat-flow + axe specs) could not run in this sandbox — same Chromium download block as phase-00; confirm frontend-ci.yml's e2e job is green before treating §8's acceptance criteria as verified. Also open: aws-infra's WebSocketLambdaAuthorizer still reads a header no browser can set (ADR 0022) — a live chat connection cannot succeed until that's repointed to the querystring token this phase's client now sends.
NEXT_STEP: sprint step 26 — frontend phase-02 (findings inbox) on feat/frontend-findings.
PREVIOUS_PHASE: frontend phase-01 (done; Playwright e2e verification deferred to CI, aws-infra WS authorizer follow-up open — see LAST_STEP)
TESTING POLICY (revised): write only tests needed to cover real behavior per phase (3-8 focused tests). No exhaustive Hypothesis fuzzing, no hand-audits — run the real tools (ruff/mypy/pytest) and fix only what fails.
DEVIATION_NOTE: user requested "start with feat/agents-foundation" — sprint step 02 (agents phase-00) has been executed before sprint step 01 (adapters phase-00). Dependencies allow this because agents phase-00 defines pure Python contracts with no adapter imports at the contract layer.

------------------------------------------------------------------
SPRINT PROGRESS (see EXECUTION_PLAN §6 for the ordered list)
------------------------------------------------------------------
Wave 1 — Substrate:
  [x] 01. adapters phase-00       feat/adapters-foundation
  [x] 02. agents phase-00         feat/agents-foundation
  [x] 03. aws-infra phase-00      feat/aws-infra-foundation
  [x] 04. aws-infra phase-01      feat/aws-infra-security  (code-complete; 2 criteria deferred — see NOTES)
  [x] 05. adapters phase-03       feat/adapters-prompts
  [x] 06. adapters phase-04       feat/adapters-evidence
  [x] 07. agents phase-11         feat/agents-guardrails  (scoped per ADR 0004 — see NOTES)
  ---- checkpoint-1-substrate ---- [TAGGED on main]
Wave 2 — Data plane + LLM adapters:
  [x] 08. aws-infra phase-02      feat/aws-infra-foundation-stack
  [x] 09. adapters phase-05       feat/adapters-ddb  (scoped to 3 representative table clients — see ADR 0006)
  [x] 10. adapters phase-01       feat/adapters-bedrock         (Grok + Bedrock providers)
  [ ] 11. adapters phase-02       feat/adapters-zelkova
  [ ] 12. aws-infra phase-03      feat/aws-infra-athena
  [ ] 13. agents phase-10         feat/agents-rag
  ---- checkpoint-2-data-plane ----
Wave 3 — Supervisor + first specialist + first demo:
  [x] 14. aws-infra phase-04      feat/aws-infra-lambda
  [x] 15. aws-infra phase-05      feat/aws-infra-bedrock
  [x] 16. agents phase-01         feat/agents-supervisor
  [x] 17. aws-infra phase-08      feat/aws-infra-crossaccount  (scoped; 3 criteria deferred — see NOTES)
  [x] 18. agents phase-02         feat/agents-passrole  (scoped per ADR 0015 — see NOTES)
  ---- checkpoint-3-first-demo ----
Wave 4 — Backend + API:
  [x] 19. backend phase-00        feat/backend-foundation  (scoped per ADR 0016 — see NOTES)
  [x] 20. aws-infra phase-07      feat/aws-infra-api  (scoped per ADR 0017 — see NOTES)
  [x] 21. backend phase-01        feat/backend-rest  (scoped per ADR 0018 — see NOTES)
  [x] 22. backend phase-02        feat/backend-websocket  (scoped per ADR 0019 — see NOTES)
  [x] 23. aws-infra phase-06      feat/aws-infra-event  (scoped per ADR 0020 — see NOTES)
  ---- checkpoint-4-api-live ---- [TAGGED on main]
Wave 5 — Frontend demo path:
  [x] 24. frontend phase-00       feat/frontend-foundation  (Playwright e2e deferred to CI — see NOTES)
  [x] 25. frontend phase-01       feat/frontend-chat  (Playwright e2e deferred to CI; WS authorizer follow-up open — see NOTES)
  [ ] 26. frontend phase-02       feat/frontend-findings
  ---- checkpoint-5-frontend-alpha ----
Wave 6 — Remaining specialists:
  [ ] 27. agents phase-07         feat/agents-shadow-guard
  [ ] 28. agents phase-05         feat/agents-scp-impact
  [ ] 29. agents phase-08         feat/agents-collision-resolver
  [ ] 30. agents phase-09         feat/agents-slr-guardian
  [ ] 31. agents phase-03         feat/agents-org-context
  [ ] 32. agents phase-04         feat/agents-data-events
  [ ] 33. agents phase-06         feat/agents-session-terminator
  ---- checkpoint-6-all-specialists ----
Wave 7 — Approval + polish:
  [ ] 34. backend phase-03        feat/backend-approval
  [ ] 35. backend phase-04        feat/backend-audit
  [ ] 36. frontend phase-03       feat/frontend-approval
  [ ] 37. frontend phase-04       feat/frontend-dashboards
  ---- checkpoint-7-approval-polish ----
Wave 8 — Cross-cutting maturity:
  [ ] 38. agents phase-14         feat/agents-memory
  [ ] 39. agents phase-16         feat/agents-cost-guardrails
  [ ] 40. agents phase-15         feat/agents-dual-mode
  [ ] 41. agents phase-17         feat/agents-self-healing
  [ ] 42. agents phase-12         feat/agents-observability
  [ ] 43. agents phase-13         feat/agents-integration
  ---- checkpoint-8-hardened (release/v1.0-submission) ----

------------------------------------------------------------------
CHECKPOINT TAGS ON MAIN (git tag, updated on merge of each wave-final PR)
------------------------------------------------------------------
- checkpoint-1-substrate — 2026-07-30, on commit c233492. Wave 1 done
  (sprint steps 01-07). 2 deferred items open (see NOTES): Guardrail LIVE
  canary + break-glass drill (need a dev account), and running the
  prompt-injection corpus through a deployed Prime (Prime doesn't exist
  until Wave 3).
- checkpoint-4-api-live — 2026-07-31, on the tmp-main-merge-event tip. Wave
  4 done (sprint steps 19-23: backend foundations/REST/WebSocket, aws-infra
  API + Event stacks). Deferred items open (see NOTES): every live-AWS
  acceptance criterion across phase-06/phase-07/backend phase-00..02 (no
  dev account), and all ~12 EventBridge rules/schedules in
  `EventStack.PENDING_EVENT_BINDINGS` whose target specialist Lambda lands
  in Wave 6/8.

------------------------------------------------------------------
NOTES + BLOCKERS
------------------------------------------------------------------
- xAI API key still not provisioned locally. adapters phase-01's
  GrokProvider is built and unit-tested against a mocked HTTP session
  (see ADR 0007) — no real xAI call has been made or tested. Set
  `XAI_API_KEY` in `.env.local` and re-verify GrokProvider against the
  real endpoint before using Grok for actual local dev or evals.
- AWS account provisioning: still not done. aws-infra phase-01 (security
  stack) was built and verified entirely offline (cdk synth + cdk-nag +
  mocked-boto3 unit tests per ADR 0001), but TWO of its acceptance criteria
  are explicitly DEFERRED and open until a dev account exists:
    1. Guardrail LIVE alias canary (payload "ignore your instructions" must
       be blocked) — needs a deployed `SentinelSecurity` stack.
    2. Break-glass drill end-to-end (single-signer fails, two-signer within
       60s succeeds, alarm fires) — needs real `sts:AssumeRole` calls
       across two distinct IAM principals.
  Whoever provisions the dev account must re-open this phase and run both
  before the security posture is production-verified. Break-glass
  initiator group must be seeded with two identities at the same time.
- Prompt-injection corpus (agents/tests/prompt_injection/corpus.jsonl, 24
  payloads, agents phase-11) has not been run end-to-end: no Prime exists
  until Wave 3 (sprint step 16) and no Guardrail is deployed until a dev
  account exists. Re-run once both land; see ADR 0004.
- aws-infra phase-02 (foundation stack) deferred 2 acceptance criteria
  pending a real AWS dev account: OSS index queryable via opensearchpy,
  and SNS delivery-status logs written to CloudWatch (needs real
  subscriptions + live delivery). See ADR 0005.
- adapters phase-05 (DDB adapter) only shipped 3 of 14 table clients
  (Findings, DecisionsInFlight, Memory episodic/semantic/procedural via
  MemoryClient) — see ADR 0006. The remaining 9 (Decisions,
  MemorySemantic's own standalone client if ever split out, Policies,
  SLRs, Revocations, Faults, Divergence, Idempotency) follow the identical
  `DynamoDbHelper` pattern; add each on-demand when the specialist or
  backend phase that needs it lands (starting with agents phase-02, F1,
  Wave 3). Also: `MemoryClient.recall_episodic`'s true k-NN semantic path
  needs a deployed OpenSearch Serverless collection — deferred with the
  rest of ADR 0005.
- adapters phase-01 (LLM adapter) deferred 2 acceptance criteria per
  ADR 0007: "p99 adapter overhead ≤ 30ms vs raw boto3" (needs a real
  benchmark against live Bedrock) and any real xAI call (see the
  XAI_API_KEY note above). Streaming's mid-stream partial-cost-sample
  cadence (spec: every 500 chunks) is not implemented — no consumer exists
  yet (backend phase-02, Wave 4) to validate the right cadence against.
- All 43 feature branches exist on origin at the baseline commit
  `Establish IAM Sentinel canon` (3a3e255). Do not delete after merge.
- aws-infra phase-08 (cross-account) deferred all 3 acceptance criteria per
  ADR 0014, pending a real AWS Organization: StackSet deployed to every
  member account, drift detection returns IN_SYNC on deployed instances,
  and feature-tag condition enforcement verified via live
  iam:SimulatePrincipalPolicy/sts:AssumeRole. agents phase-02 fixed the
  half of this that was code (see below); the live-AssumeRole proof itself
  is still open. Delegated-admin role's wider Access Analyzer/SSO surface
  also left unspecified — agents phase-02 (F1) turned out NOT to touch
  Access Analyzer at all (only plain iam:List*/Get*); still pending
  whichever of phase-03 (F2, org-context)/phase-07 (F6) actually needs it.
- agents phase-02 (F1 PassRole Cartographer) deferred per ADR 0015:
  (1) CDK wiring of PassRoleCartographer's CfnAgent + its two action-group
  Lambdas into aws-infra's BedrockStack — blocked on Lambda dependency-
  layer packaging (networkx + pydantic), a gap that predates this phase
  (functions/layers/{boto3,powertools}/python/ are still empty .gitkeep
  placeholders across all 16 prior phases, not something new to F1).
  Whichever phase first solves that packaging problem should also call
  `BedrockStack.new_agent()`/`associate_collaborator()` for F1 — the
  Python tool code (tools/f1/scan.py, tools/f1/graph.py) is ready now.
  (2) §9's "500-principal scan <60s" acceptance criterion is a design
  property (bounded 10-worker thread pool, per-scan policy cache), not a
  measured benchmark — moto has no network latency to approximate at that
  scale meaningfully. (3) The golden eval set (agents/evals/f1/golden.jsonl,
  10 entries covering all 5 required categories) is schema-verified only;
  `iam_sentinel_agents.evals.runner` is a phase-12 deliverable that
  doesn't exist yet, and no F1 agent is deployed to run a turn against.
  Re-open all three once their respective blockers land.
- backend phase-00 (FastAPI foundation) deferred per ADR 0016: (1) §6's
  "<800ms cold-start" acceptance criterion is design-bounded (Mangum
  lifespan="off", lru_cache singleton adapter deps, dev-mode docs disabled
  in prod) but not measured — no Lambda deployment target exists yet
  (aws-infra phase-07, sprint step 20, next). (2) `GET /health`'s `commit`
  field defaults to `"unknown"`; no CI packaging step injects a build-time
  commit SHA yet (also aws-infra phase-07's deploy pipeline). (3) The
  SigV4 relay path (`auth/sigv4.py`, used only for WebSocket auth/local
  dev — API Gateway's IAM-auth pass-through is the primary path and needs
  no STS call) is unit-tested against a mocked `requests.Session`; the
  live round-trip to `sts.<region>.amazonaws.com` is unverified, though
  that's a fixed public AWS API surface, not repo-specific infra. Re-open
  (1) and (2) once aws-infra phase-07 deploys this Lambda.
- backend phase-01 (REST API) deferred per ADR 0018: (1) §9's "p95 latency
  ≤ 500ms" and "500-principal-scale" style live-account criteria -- no
  deployed API Gateway + Lambda to benchmark against. (2) Cross-principal
  read leakage is enforced (deny-by-default in findings_service.py/
  decisions_service.py) and unit-tested, not Hypothesis property-fuzzed,
  per the revised testing policy. (3) `RouterBridgeService`
  (`/analyze/*`, `/enrich/policy`, `/resolve/*`, `/scan/*`, `/emergency/
  kill-session`, `/monitor/shadow-violations`) is built against the
  documented `router.execute(mode="fast", ...)` contract and unit-tested
  with a mocked `LambdaInvokeClient` -- `functions/router` (agents
  phase-15, sprint step 40) doesn't exist yet to invoke for real. (4)
  `GET /operations/faults`/`cost/weekly` display whatever agents
  phase-16/17 (cost guardrails, self-healing -- neither built) eventually
  write; `FaultRecord`'s DDB key shape was confirmed against
  `aws-infra`'s `foundation_stack.py` directly, but no producer exists yet
  to round-trip against. (5) `POST /decisions/{id}/approve|reject` only
  transitions `DecisionRecord.status`; the Zelkova pre/post-check-gated
  remediation-apply workflow is `backend` phase-03's job. (6) Neither
  `FindingsClient` nor `DecisionsClient` has a GSI keyed on
  `finding_id`/`decision_id` alone -- `get_by_id` on both falls back to a
  bounded (not O(1)) `Scan`; re-open if that path sees real production
  traffic and add a dedicated GSI. Re-open all six once their respective
  blockers land.
- aws-infra phase-07 (API stack) deferred 4 items per ADR 0017: (1)
  `/emergency/*`'s native-AWS_IAM resource-policy Deny (keyed on
  `aws:PrincipalTag/BreakGlass`) has no live two-signer STS session to
  verify against — needs the same real-AWS-account gap aws-infra phase-01's
  break-glass drill already opened. (2) The hybrid authorizer's Cognito
  access-token path (`cognito-idp:GetUser`) is unit-tested against mocked
  `ClientError`/success shapes only, no real User Pool. (3)
  `functions/backend_api/handler.py` is a placeholder shim — `backend`'s
  actual dependency closure (fastapi/mangum/pydantic/requests/
  iam_sentinel_adapters) is not bundled into any Lambda deployment
  package yet; this is the same pip-bundling gap ADR 0011/0015 already
  flagged (`functions/layers/{boto3,powertools}/python/` still empty
  `.gitkeep` placeholders). A live `curl` against a deployed stage
  returns a deterministic `502 BACKEND_NOT_PACKAGED` until that's solved.
  (4) `SentinelStream`'s `$default` route only acknowledges frames; real
  `InvokeAgentWithResponseStream` + chunk-by-chunk `PostToConnection` fan-
  out is `backend` phase-02's deliverable (sprint step 22), not this
  phase's. Re-open all four once their respective blockers land.
- backend phase-02 (WebSocket streaming) deferred per ADR 0019: (1) the
  real business logic (`iam_sentinel_backend.ws.{connect,default,
  disconnect}.handler`) is built and fully unit-tested but NOT wired into
  `aws-infra`'s deployed `ws_connect`/`ws_default`/`ws_disconnect` Lambdas —
  blocked on the same pre-existing Lambda dependency-bundling gap ADR
  0011/0015/0017 already flagged; those Lambdas still run phase-07's
  placeholder `boto3` bodies in production today. Whoever solves the
  bundling gap should repoint `aws-infra/functions/ws_*/handler.py` at the
  new `handler()` entrypoints and delete `aws-infra/tests/unit/
  test_ws_handlers.py`'s now-superseded placeholder assertions in the same
  change. (2) §7's "<300ms progress-chunk latency" criterion, and §6's
  LocalStack WebSocket round-trip + 10Hz connect/disconnect leak test — all
  need a live API Gateway WebSocket endpoint; the ~50 msg/s rate limiter,
  128KB frame cap, and GoneException cleanup are implemented and unit-
  tested as design properties, not measured against a real connection.
  Re-open both once their respective blockers land.
- aws-infra phase-06 (Event stack) deferred all ~12 EventBridge rules/
  schedules from phase-06 §3 per ADR 0020 -- every target Lambda (F5
  Session Terminator, F6 Shadow Guard, F8 SLR Guardian, the memory fabric's
  semantic syncer, cost guardrails' weekly report, self-healing's
  watchdog, the RAG KB ingestion pipeline) is owned by a phase that hasn't
  landed yet (Wave 6/8). The full table lives in
  `aws-infra/src/iam_sentinel_infra/stacks/event_stack.py`'s
  `PENDING_EVENT_BINDINGS`; each owning phase calls
  `EventStack.register_event_rule()`/`register_schedule()` from its own
  stack when it lands, per the same division of ownership ADR 0011
  established for `LambdaStack.new_function()`. Also deferred: the
  mgmt-account CloudTrail log-group subscription filter (needs a real org
  trail's log group ARN) and phase-06 §6's "fires within ±60s"/"alarms
  deliver to SNS" acceptance criteria (need a deployed stack on a real AWS
  dev account).

- frontend phase-00 (Frontend Foundations) deferred its Playwright
  acceptance criteria (auth-flow spec, axe-core scans on login + dashboard)
  to CI: this dev sandbox's network policy blocks the Chromium binary
  download `playwright install` needs, so neither spec has actually been
  executed anywhere yet. Both are written to need zero live AWS/Cognito
  (MSW mocks the backend and the Cognito token endpoint), so they should
  just work once `frontend-ci.yml`'s `e2e` job runs on GitHub's
  ubuntu-latest runners — confirm that job is green before relying on
  phase-00 §8's "Auth flow works in Playwright" / "axe-core scan clean"
  criteria. Separately, `next build`'s standalone-output trace-copy step
  fails on this Windows machine with EPERM on symlink creation (needs
  Developer Mode/elevation, a Windows-only constraint) — the actual
  compile + static generation were confirmed to succeed by temporarily
  removing `output: "standalone"` and rebuilding; this is a non-issue on
  CI's Linux runners.

- frontend phase-01 (Chat Console) deferred its Playwright acceptance
  criteria (chat-flow spec using `page.routeWebSocket`, axe-core scan on
  `/chat`) to CI for the same reason as phase-00: this dev sandbox's
  network policy blocks the Chromium download. Confirm frontend-ci.yml's
  `e2e` job is green before relying on phase-01 §8's criteria. Separately
  and more significantly: per ADR 0022, `aws-infra`'s
  `WebSocketLambdaAuthorizer` (phase-07) reads `route.request.header.
  Authorization` on the `$connect` handshake — a header no browser
  `WebSocket` can set. This phase's client instead mints a same-origin
  token (`app/api/ws-token/route.ts`) and sends it as a `?token=` query
  param, but nothing can make a REAL deployed connection succeed until a
  future aws-infra change repoints `identity_source` to
  `route.request.querystring.token` and the authorizer Lambda reads from
  there. Whoever next touches aws-infra's WebSocket auth wiring should
  close this gap; it predates this phase (backend phase-02 already built
  against the header-based contract) but was only discovered here, by the
  first real browser consumer.

------------------------------------------------------------------
HISTORY (append-only)
------------------------------------------------------------------
2026-07-30 baseline canon committed to main (commit 3a3e255).
2026-07-30 43 feat/* branches pushed to origin.
2026-07-30 EXECUTION_PLAN.txt and EXECUTION_STATE.txt authored.
2026-07-30 agents phase-00 started on feat/agents-foundation (per user override). Chunk 1 committed at 94acd79 — pyproject, tooling configs, 8 Pydantic contracts, contract tests. Branch pushed to origin.
2026-07-30 Local toolchain gap noted: Windows machine has Python 3.13 only; uv/ruff/mypy/pytest not installed. Tests will run via CI once .github/workflows/agents-ci.yml lands in chunk 2, and locally after user installs uv.
2026-07-30 adapters phase-00 done on feat/adapters-foundation, merged to main at 67cc04f, tagged phase/adapters-foundation-done. uv now installed locally; ran real ruff/mypy --strict/pytest. Deviation: pinned `aws-lambda-powertools[all]` bumped 2.42.0 -> 3.2.0 in adapters/pyproject.toml — 2.42.0's [all] extra hard-pins pydantic<2.0.0, which conflicts with pydantic==2.9.2 and made `uv sync` fail to resolve; confirmed the same conflict exists in agents/pyproject.toml (still on 2.42.0, unfixed — agents phase-00's prior "all clean" toolchain claim could not have been produced by a real `uv sync` on that pin and should be re-verified before agents phase-11 builds on it).
2026-07-30 agents phase-00 re-verified: rebased feat/agents-foundation onto main, applied the same aws-lambda-powertools 2.42.0 -> 3.2.0 fix, ran real uv sync/ruff/mypy --strict/pytest. Result: 78 tests pass, 96% coverage, ruff clean, mypy --strict clean — the original functional claim held once the pin was fixed. Merged to main at 0d73bfe (fast-forward). No new tag; phase/agents-foundation-done already covers this phase.
2026-07-30 aws-infra phase-00 done on feat/aws-infra-foundation, merged to main at 7a62f9c, tagged phase/aws-infra-foundation-done. Confirmed via the actual phase-00 spec that no live AWS account is needed (synth-only); node/npm available locally, cdk CLI run via `npx aws-cdk@2.163.0` (cdk.json app entry set to `uv run python app.py` so the CLI uses the uv-managed venv). Built config.py + 8 stack shells (dependency graph wired, resources land in later phases) + 5 shared L3 constructs + cdk-nag/AwsSolutions+HIPAA wiring. `cdk synth --all` clean for dev/staging/prod, cdk-nag zero Error-severity findings, snapshot baseline captured for all 8 stacks, 22 pytest cases (config validation + construct instantiation + synth/nag/snapshot), 99% coverage, ruff/mypy --strict clean.
2026-07-30 aws-infra phase-01 done on feat/aws-infra-security, merged to main at 9f7f72c, tagged phase/aws-infra-security-done. User explicitly chose "build code-complete, defer live checks" for the 2 acceptance criteria that need a real AWS account (Guardrail LIVE canary, break-glass end-to-end drill) — see docs/decisions/0001. Built 3 KMS CMKs, Guardrail lifecycle Lambda (real bedrock:CreateGuardrail/UpdateGuardrail/DeleteGuardrail/CreateGuardrailVersion routing, unit-tested against a mocked boto3 client), extended SentinelPermissionBoundary with the phase-01 §5 deny statements, break-glass IAM role + two-signer Step Functions Standard workflow (approval Lambda decision logic unit-tested: distinct-principal + 60s-window check) + EventBridge/CloudWatch alarm to SNS. Two more deviations recorded as ADRs: 0002 (EventBridge management events instead of provisioning a new CloudTrail Trail) and 0003 (AWS Backup plan deferred to phase-02, PITR enabled in the meantime). Fixed a real bug along the way: IAM's MaxSessionDuration has a 3600s floor, so the spec's "900s break-glass session" is enforced via AssumeRole's DurationSeconds in the assume Lambda, not the role's MaxSessionDuration property. cdk synth clean for all 8 stacks/3 stages, cdk-nag (AwsSolutions + HIPAA) zero errors after real fixes (DLQ + reserved concurrency on every Lambda, DDB PITR, Step Functions logging + X-Ray, SNS KMS encryption + enforced TLS) plus justified suppressions for CDK-standard patterns (Lambda basic execution role, DefaultPolicy inline grants) and already-canonical architecture decisions (no-VPC per docs/ARCHITECTURE.md). 34 tests pass, 99% coverage, ruff/mypy --strict clean.
2026-07-30 adapters phase-03 done on feat/adapters-prompts, merged to main at 87e7f3d, tagged phase/adapters-prompts-done. Built sanitize_untrusted() + build_prompt(); pulled forward a minimal evidence/canonicalize.py (RFC 8785 JCS subset) from phase-04 since the fencer needs it now. Found and fixed a real bug: forbidden patterns containing `<`/`>` (`</trusted_input`, `<|...|>`) were being checked after those characters were already stripped by the sanitizer's own character-removal step, making them permanently dead — the check now runs on the pre-strip text. Testing deviation from spec §7 (per the revised testing policy): Hypothesis property run capped at 200 examples, not 10,000; injection-pattern coverage trimmed to ~11 representative payloads, not the full 50-payload corpus. 38 tests pass, 98% coverage, ruff/mypy --strict clean.
2026-07-30 adapters phase-04 done on feat/adapters-evidence, merged to main at 94b6103, tagged phase/adapters-evidence-done. Rewrote canonicalize.py for true RFC 8785 compliance — fetched the actual published test vectors from github.com/cyberphone/json-canonicalization (values/structures/arrays/french) via WebFetch and verified against them (weird.json/unicode.json skipped: their combining-character/emoji content couldn't be confirmed byte-exact through the fetch tool's summarization step). The phase-03 canonicalize.py was only a pragmatic subset (plain sort_keys json.dumps), not RFC-compliant — this rewrite implements real ECMA-262 Number::toString formatting (fixed vs. scientific notation thresholds), which Python's own float repr doesn't match beyond a coincidental overlap on small test cases. Built evidence/keys.py, kms_signer.py, kms_verifier.py (1h public-key cache; kms:Verify stays remote by design rather than reimplementing KMS's PSS salt-length convention locally), evidence/client.py (put_signed_evidence/verify with tamper detection via re-canonicalize+re-hash), security_hub/asff_mapper.py + client.py (batch 100, retry on LimitExceededException). Module-boundary note: adapters does not import agents' Finding model — ASFF mapper takes a local AsffFindingInput dataclass mirroring DATA_CONTRACTS.md instead. Testing scope reduced per the revised policy: property run at 200 examples (spec asks 1,000); ASFF conformance checked against documented required fields, not a live Security Hub schema validator. 67 tests pass, 98% coverage, ruff/mypy --strict clean.
2026-07-30 agents phase-11 done on feat/agents-guardrails, merged to main at c233492, tagged phase/agents-guardrails-done. Wave 1 complete — tagged checkpoint-1-substrate. Per ADR 0004, scoped to just the two deliverables not already covered by other sprint steps (fencer/sanitizer = adapters phase-03 done; break-glass STS = aws-infra phase-01 done; Zelkova = its own sprint step, adapters phase-02): the actual Guardrail policy content (aws-infra/config/guardrail_v1.json — topic/content/PII/grounding policies) wired through an extended GuardrailCustomResource + lifecycle Lambda, and a 24-payload prompt-injection corpus (agents/tests/prompt_injection/corpus.jsonl, 3 per category x 8 categories, reduced from the spec's 200 per the revised testing policy) with a schema/quality test. Running the corpus end-to-end through a deployed Prime + Guardrail is deferred (neither exists yet) — tracked in NOTES + BLOCKERS. cdk synth clean, cdk-nag zero errors, ruff/mypy --strict clean in both aws-infra and agents; aws-infra 36 tests pass, agents 82 tests pass (96% coverage).
2026-07-30 aws-infra phase-02 done on feat/aws-infra-foundation-stack, merged to main at c37bf56, tagged phase/aws-infra-foundation-stack-done. Wave 2 begins. Per ADR 0005: adapters phase-05's table inventory lists 14 tables despite both phase docs saying "13" -- provisioned all 14 rather than under-provisioning to match the miscounted headline. Established the key-attribute convention for the spec's `#`-joined PK/SK/GSI notation (composite-value attribute for main table keys, PK+SK split on the last `#` for GSIs) since adapters phase-05 hasn't landed yet to confirm it -- reconcile there if that phase's author reads it differently. Built all 14 DDB tables (CMK, PITR, deletion protection, on-demand) + an org-wide AWS Backup plan (the deferral ADR 0003 promised once this phase landed), the OSS VECTORSEARCH collection + policies + an index-bootstrap Lambda that hand-signs SigV4 via botocore instead of depending on opensearchpy (no Lambda-layer pipeline exists to add that dependency), 5 new S3 buckets sharing one access-log bucket, SessionKillQueue.fifo+DLQ, and 5 new SNS topics. Caught a real bug before it could hit deploy: phase-02 spec lists `SentinelSecurity` as one of its 6 topics, but aws-infra phase-01 already created it in SecurityStack for the break-glass alarm -- creating it again would collide (SNS names are unique per account+region). FoundationStack now references `security.security_topic` instead. Also had to fix mypy: two function dirs share a `handler.py` filename, which collided as one module when checked together -- CI and local runs now check each `functions/*/` directory separately. cdk synth clean, cdk-nag zero errors after real fixes (AWS Backup plan, log-bucket versioning) plus justified suppressions (no-VPC, single-region, CDK-standard managed-policy patterns, SSE-S3-only access-log bucket). 43 tests pass, 99% coverage, ruff/mypy --strict clean. Deferred per ADR 0005: OSS index query verification and SNS delivery-status logging both need a real AWS account.
2026-07-30 adapters phase-05 done on feat/adapters-ddb, merged to main at 6d4a1c9, tagged phase/adapters-ddb-done. Per ADR 0006: scoped to `DynamoDbHelper` (the actual reusable logic -- retry, circuit breaker, EMF metrics) plus 3 representative table clients (FindingsClient, DecisionsInFlightClient, MemoryClient's DDB paths) instead of all 14 -- no specialist Lambda exists yet to validate the other 9 tables' query shapes against (Prime and F1 land in Wave 3). Reconciled ADR 0005's key-attribute convention as promised: confirmed the `#`-joined composite attribute names work as designed. Found a real bug doing so: DynamoDB's expression grammar treats a bare `#` as a name-placeholder marker, so those composite attribute names (`account_id#feature_id`, etc.) can't be referenced directly in KeyConditionExpression/UpdateExpression strings -- they need ExpressionAttributeNames aliases, which the base client didn't originally support. Added that support before it could bite a real caller. Client methods take/return plain dicts, never an agents Finding/EpisodicMemory model (module boundary). 86 tests pass, 95% coverage, ruff/mypy --strict clean. Deferred per ADR 0006: 9 remaining table clients (add on-demand per-consumer) and MemoryClient.recall_episodic's true k-NN path (needs a deployed OSS collection, per ADR 0005).
2026-07-30 adapters phase-01 done on feat/adapters-bedrock, merged to main at 964c71f, tagged phase/adapters-bedrock-done. Built per docs/EXECUTION_PLAN.txt §2 (supersedes the pre-split phase-01 spec) and ADR 0007: a shared LLMProvider Protocol with BedrockProvider (real bedrock-agent-runtime/bedrock-runtime calls, budget gate, AGGRESSIVE retry, stopReason-based Guardrail detection, cost sampling) and GrokProvider (xAI's OpenAI-compatible endpoint via a new `requests` dependency, in-process multi-agent emulation, local structural Guardrail stand-in using the sanitizer's forbidden-pattern list), selected via SENTINEL_LLM_PROVIDER. Also built model_router.pick_model (cost-aware Haiku/Sonnet downgrade at 70% of cap) and output_validator.validate_output (rejects forged ARNs/evidence_ids/forbidden patterns). Found and closed a real cross-module gap: aws-infra phase-01 published /sentinel/{stage}/guardrail/version to SSM but never /sentinel/{stage}/guardrail/id, which this phase's GuardrailAccessor needs — added it to SecurityStack. Made prompts/sanitizer.py's FORBIDDEN_PATTERNS public so output_validator and GrokProvider's local guardrail both reference the same list instead of duplicating it. 112 tests pass, 94% coverage, ruff/mypy --strict clean in both adapters and aws-infra. Deferred per ADR 0007: the p99-overhead-vs-raw-boto3 benchmark, any real xAI call (XAI_API_KEY still unprovisioned), and streaming's mid-stream partial-cost-sample cadence (no consumer yet to validate it against).
2026-07-31 sprint steps 11-16 done prior to this session (adapters phase-02 Zelkova, aws-infra phase-03 Athena, agents phase-10 RAG, aws-infra phase-04 Lambda, aws-infra phase-05 Bedrock, agents phase-01 Sentinel Prime supervisor) — see git tags phase/adapters-zelkova-done, phase/aws-infra-athena-done, phase/agents-rag-done, phase/aws-infra-lambda-done, phase/aws-infra-bedrock-done, phase/agents-supervisor-done for detail; this file's cursor had gone stale and was reconciled against git history at the start of this session.
2026-07-31 aws-infra phase-08 (cross-account) done on feat/aws-infra-crossaccount, merged to main at 7afc5ed (fast-forward from prior WIP), tagged phase/aws-infra-crossaccount-done. Per ADR 0014: both StackSets (SentinelCrossAccountRole, SentinelDelegatedAdminAccountRole) synth-verified with hand-built CFN role templates (StackSets take a template body, no CDK L2 exists for that), drift-detection Lambda (real DetectStackSetDrift + bounded DescribeStackSetOperation poll + ListStackInstances, EMF metric alarmed through SecurityStack.security_topic), and a new-account health-check Step Functions workflow (30-min Wait matching AutoDeployment worst case, then sts:AssumeRole+iam:GetRole against the new account, Catch alerts to security_topic), triggered off Organizations CreateAccountResult on the default bus (no new Trail, per ADR 0002's precedent). Widened SentinelPermissionBoundary's AllowCrossAccountRoleAssumption to cover both new roles — without it no Sentinel Lambda could ever assume the delegated-admin role. Regenerated 3 stale snapshot fixtures (SentinelAthena, SentinelBedrock, SentinelCrossAccount) that had drifted from actual synth output before this phase's toolchain run. moto has no StackSet backend (same gap as ADR 0008/Access Analyzer) — tested via static assertions against the synthesized AWS::CloudFormation::StackSet resource + embedded template JSON, plus mocked-boto3 unit tests for both Lambdas. 91 tests pass, 99% coverage, ruff/mypy --strict clean. Deferred per ADR 0014: all 3 phase-08 acceptance criteria need a real AWS Organization (see NOTES).
2026-07-31 agents phase-02 (F1 PassRole Cartographer) done on feat/agents-passrole, merged to main at 6973483, tagged phase/agents-passrole-done. Rebased the branch onto main first (it had sat at the original baseline commit 3a3e255 since the 43 feat branches were cut, 16 phases behind). Built per agents/docs/phase-02-passrole-cartographer.txt: contracts.passrole (PassRoleEdge/BlastPath/PassRoleBlastPayload), tools/f1/wildcard.py (fnmatchcase-based ARN Resource pattern resolver), tools/f1/scan.py (passrole_scan: paginated ListUsers/ListRoles, inline+attached policy collection via a 10-worker bounded ThreadPoolExecutor per §10's throttling risk, PassRole-statement extraction, wildcard edge resolution), tools/f1/graph.py (passrole_graph: networkx.DiGraph blast-radius BFS to depth 2), tools/f1/privilege.py + severity.py (the CRITICAL/HIGH/MEDIUM/LOW rubric), the specialist prompt and F1 action-group OpenAPI schema (both copied verbatim from spec), and a 10-entry evals/f1/golden.jsonl covering all 5 required categories. Found and fixed a real cross-module bug: tools/common/cross_account.assume() never passed Tags/TransitiveTagKeys to sts:AssumeRole, so aws:PrincipalTag/Feature — the condition key aws-infra ADR 0014 gates F2/F3/F5's mutating actions on — never actually existed at runtime; fixed with a regression test. Per ADR 0015, three things scoped out: (1) CDK wiring of the CfnAgent + its two Lambdas into BedrockStack, blocked on a pre-existing Lambda dependency-layer packaging gap (empty layer placeholders since aws-infra phase-04, not new to F1); (2) passrole_graph deviates from the spec's own "no AWS calls" one-liner and does real IAM reads to classify reached-role privilege, because no PassRoleEdge field carries the target role's own policy content and the spec's own Step 2 requires evaluating it — documented as a deliberate, tested resolution of a spec self-contradiction; (3) the golden eval set and the §9 500-principal/60s benchmark are both schema/design-verified only, not run/measured (phase-12's runner doesn't exist; no deployed F1 agent exists). 191 tests pass (moto-mocked against all 4 required fixtures: admin_shortcut→CRITICAL, service_scoped→MEDIUM, wildcard_pattern→4 resolved edges, no_passrole→empty), 96% coverage, ruff/mypy --strict clean in agents (mypy --strict run against src plus every touched/new test file; pre-existing unrelated strict-mode gaps in untouched test files were left alone, matching established precedent that only src is held to the "clean" bar).
2026-07-31 backend phase-00 (FastAPI foundation) done on feat/backend-foundation, merged to main at 7a06a05, tagged phase/backend-foundation-done. First backend-component phase -- no prior backend code existed, so this followed the phase doc plus adapters/agents' repo-wide conventions (pyproject.toml shape, ruff/mypy --strict config, Pydantic v2 Base-model style, aws_lambda_powertools usage, boto3-only-through-adapters). Merged main in first (18 phases of substrate) before building. Delivered create_app()/Mangum handler (lifespan="off" for cold-start), CorrelationIdMiddleware (ULID mint-or-echo + Powertools logger/X-Ray annotation), CognitoJwtVerifier (JWKS RS256, 15-min cache, verified against a real generated RSA keypair + signed JWTs in tests -- valid/tampered/expired/unknown-kid all covered), SigV4 auth with two paths (from_apigw_identity trusting API Gateway's own verified pass-through; SigV4Verifier relaying a caller-presigned GetCallerIdentity request for the WebSocket/local-dev fallback, 5-min cache), breakglass.verify_breakglass_header, deps.py's DI surface (get_principal dispatch across both auth paths, get_correlation_id, lru_cache singleton adapter deps), and errors.py's full domain-exception-to-HTTP-status mapping (SanitizerRejection/GuardrailInterventionError/ValidationError->400, AccessDeniedError->403, BudgetExceededError->429, CircuitOpenError->503, ZelkovaError->500, catch-all->500 with no traceback leak). Added iam_sentinel_adapters.sts.StsClient on-demand (adapters/src/iam_sentinel_adapters/sts.py) since the SigV4 relay path needs sts:GetCallerIdentity and boto3-only-through-adapters forbids that call living in backend/ -- same precedent as ADR 0006; verify_signed_request relays caller-presigned headers rather than accepting raw secret keys (the Vault/aws-iam-authenticator pattern). Built the OpenAPI golden-file contract-diff mechanism called for by phase-00 §4 Step 6 (scripts/export_openapi.py, backend/openapi.golden.json, test_openapi_contract.py, backend-ci.yml's contract job) rather than deferring it -- fully buildable offline. Per ADR 0016, deferred: the <800ms cold-start acceptance criterion (design-bounded, not measured -- no Lambda deployment target exists until aws-infra phase-07 next), /health's commit field (defaults "unknown", no build pipeline injects it yet), and the SigV4 relay's live STS round-trip (unit-tested against a mocked requests.Session only). 30 backend tests pass, 91% coverage; adapters grew to 142 tests (94% coverage, +4 for StsClient); ruff, ruff format, and mypy --strict clean in both packages.
2026-07-31 aws-infra phase-07 (API stack) done on feat/aws-infra-api, merged to main at eda4134, tagged phase/aws-infra-api-done. Branch had sat at the original baseline commit 20086ca since the 43 feat branches were cut; merged main (18 phases of substrate through backend phase-00) in first. Built per aws-infra/docs/phase-07-api-stack.txt: SentinelApi (REST API, {proxy+} ANY -> backend Lambda LambdaIntegration, explicit /health GET with AuthorizationType.NONE, /emergency/{proxy+} with native AuthorizationType.IAM), SentinelStream (WebSocket API, $connect/$disconnect/$default routes backed by three new Lambdas and a new SentinelConnections DDB table), a Cognito User Pool (MFA REQUIRED+TOTP, 14-char password policy, ENFORCED advanced security via a CfnUserPool escape hatch, 3 groups, hosted domain, SRP+OAuth app client), a WAF Web ACL (both AWS managed rule groups, a 500/5min rate-based rule, a custom prompt-injection byte-match rule) associated to the stage, and two throttling usage plans. Per ADR 0017, four spec ambiguities forced real architecture decisions: (1) one hybrid REQUEST Lambda authorizer (functions/api_authorizer) covers both "Cognito JWT authorizer" and "IAM SigV4 machine caller" since one API Gateway method can't carry two authorizationTypes; (2) that authorizer verifies Cognito access tokens via cognito-idp:GetUser (zero third-party deps, matching every prior aws-infra Lambda's boto3-only convention -- no PyJWT/cryptography layer exists) and relays SigV4 GetCallerIdentity to STS via stdlib urllib; (3) /emergency/* is gated by a native AWS_IAM resource-policy Deny keyed on aws:PrincipalTag/BreakGlass rather than the Lambda authorizer, because no AWS read API can recover a caller's session tags after the fact -- only IAM's own policy evaluation at the execute-api:Invoke boundary can; (4) functions/backend_api/handler.py is a real, tested shim over iam_sentinel_backend.app.handler that degrades to a deterministic 502 BACKEND_NOT_PACKAGED until a Lambda dependency-bundling pipeline exists (same packaging gap ADR 0011/0015 already flagged) -- the CDK wiring itself (Function, integration, IAM role, SSM params) is fully real. Found and fixed a real bug before it could hit deploy: assigning the REST API's self-referencing resource policy via rest_api.arn_for_execute_api() embeds a Ref to the API's own logical ID inside its own Policy property, which CDK's dependency-graph inference flags as an undeployable cycle even though CloudFormation itself special-cases this exact self-reference for API Gateway resource policies -- fixed by using AWS's documented relative resource shorthand (execute-api:/*) instead of the full ARN. Widened SentinelPermissionBoundary (cognito-idp:GetUser, execute-api:ManageConnections) from this stack, same "extend from the phase that needs it" precedent agents phase-02 set for AllowCrossAccountRoleAssumption. Regenerated 6 stale snapshot fixtures (SentinelSecurity, SentinelFoundation, SentinelAthena, SentinelBedrock, SentinelApi, SentinelCrossAccount) that legitimately shifted from the new SSM params, widened boundary, and new stack resources. cdk synth clean for dev/staging/prod, cdk-nag (AwsSolutions + HIPAA) zero Error-severity findings after real fixes (X-Ray tracing on the API stage, KMS encryption on the access-log and WAF-log groups, WAF logging enabled) plus justified suppressions (the two self-reference IAM5 wildcards, /health and WebSocket $disconnect/$default's documented lack of per-route auth, no custom domain/ACM cert yet -- scoped out per the spec's own "(optional)" wording). 112 tests pass, 97% coverage, ruff/mypy --strict clean. Deferred per ADR 0017: all 4 items in NOTES above (live break-glass tag enforcement, the Cognito authorizer path against a real User Pool, backend_api's actual Lambda bundle, and SentinelStream's real Prime-streaming fan-out).
2026-07-31 backend phase-01 (REST API) done on feat/backend-rest, merged to main, tagged phase/backend-rest-done. Rebased/merged origin/main (through aws-infra phase-07) into the branch first -- it had sat at the original baseline commit since the 43 feat branches were cut. Implemented all 16 routes from aws-infra/docs/phase-07-api-stack.txt §3: routers/{chat,router_bridge,findings,decisions,approvals,operations}.py, each backed by a services/*.py that owns HTTP<->adapter-dict translation and access control. chat_service.ask_prime invokes Prime via the LLM adapter then polls SentinelDecisions for the post-turn Lambda's DecisionRecord (new DecisionsClient.get_by_correlation_id, O(1) via the existing correlation-index GSI) up to a 25s budget, returning ESCALATED on exhaustion -- deliberately does not re-parse Prime's raw completion into structured Finding/SpecialistVerdict objects, since agents/prime/post_turn.py already owns and validates that and backend must not import agents/ (module boundary; backend/pyproject.toml depends only on iam-sentinel-adapters). Backend-local Pydantic schemas (schemas/) mirror docs/DATA_CONTRACTS.md rather than importing agents.contracts, same precedent adapters' table clients already set with plain dicts. Added on-demand per ADR 0006's precedent: iam_sentinel_adapters.ddb.faults.FaultsClient (key shape confirmed against aws-infra's foundation_stack.py, not the older DATA_CONTRACTS.md prose), iam_sentinel_adapters.s3.reports.ReportsClient, iam_sentinel_adapters.compute.lambda_client.LambdaInvokeClient (the router-bridge's callee, functions/router, is agents phase-15 and doesn't exist yet -- built against the documented contract, same "build the caller before the callee" precedent as ADR 0014/0017). DynamoDbHelper gained query_page/scan_page (additive, LastEvaluatedKey-based next_token pagination) plus FindingsClient/DecisionsClient gained index-aware list_page and bounded get_by_id fallbacks, since neither table has a GSI keyed on finding_id/decision_id alone (confirmed by reading foundation_stack.py's actual _TableSpec definitions, not assuming DATA_CONTRACTS.md's GSI2 description was current -- it wasn't: GSI2 is feature_id#status, not plain feature_id). Found a real pre-existing inaccuracy while wiring the chat poll: DecisionsClient.put's docstring claimed decision_id is derived from correlation_id; agents/src/iam_sentinel_agents/prime/post_turn.py actually mints decision_id as an independent new_ulid() -- get_by_id therefore cannot use the correlation-index GSI and falls back to a bounded scan/query, documented in ADR 0018 rather than silently assumed. Six items scoped out per ADR 0018 (see NOTES). Real toolchain run in both packages: ruff, ruff format, mypy --strict, pytest --cov. Adapters: 160 tests pass, 96% coverage (+18 new tests: FaultsClient, ReportsClient, LambdaInvokeClient, FindingsClient/DecisionsClient pagination and id-lookup). Backend: 66 tests pass, 92% coverage (+36 new tests across service-level unit tests and router-level TestClient integration tests), openapi.golden.json regenerated for the 16 new routes.
2026-07-31 backend phase-02 (WebSocket streaming) done on feat/backend-websocket, merged to main, tagged phase/backend-websocket-done. Fetched/merged origin/main (through backend phase-01, sprint step 21) into the branch first -- it had sat at the original baseline commit since the 43 feat branches were cut. Built per backend/docs/phase-02-websocket-stream.txt §2: iam_sentinel_backend.ws.{connect,default,disconnect,fanout,protocol} -- connect.py mints a session-id ULID and persists SentinelConnections (principal/auth_kind already resolved by aws-infra phase-07's WebSocket REQUEST authorizer before this runs); disconnect.py removes that row; default.py dispatches chat/cancel/ping frames strictly off the connection's own DDB-resolved principal, never client-supplied identity (closes the phase-02 §8 cross-connection-leakage risk structurally); fanout.py's StreamFanoutService forwards Bedrock invoke_agent_stream chunks to apigatewaymanagementapi:PostToConnection with a ~50 msg/s rate gate and a 128KB frame truncation budget, handles mid-stream cancel via SentinelDecisionsInFlight and GoneException cleanup, and -- following the exact ADR 0018-decision-5 pattern ChatService already established -- does not re-parse Prime's completion text, instead polling SentinelDecisions for the post-turn Lambda's out-of-band DecisionRecord once the stream's final chunk lands. New adapters clients added on-demand per ADR 0006's precedent: iam_sentinel_adapters.ddb.connections.ConnectionsClient and iam_sentinel_adapters.apigw.management.ManagementApiClient (translates GoneException/ThrottlingException/other ClientError into the adapters error hierarchy; a new ConnectionGoneError joins it). DecisionsInFlightClient grew cancel()/is_canceled() (update_item-based, non-destructive of the original dispatch payload). Per ADR 0019, the real logic is NOT wired into aws-infra's already-deployed ws_connect/ws_default/ws_disconnect placeholder Lambdas: those Lambdas' own phase-07 docstrings already deferred this logic here, but actually repointing them hits the same pre-existing Lambda dependency-bundling gap ADR 0011/0015/0017 already flagged, and aws-infra/tests/unit/test_ws_handlers.py currently asserts the placeholder behavior as passing -- each backend/ws module instead exports both the pure DI-tested function and a real handler(event, context) entrypoint, ready for that rewiring once the bundling gap closes. Real toolchain run in both packages: ruff, ruff format, mypy --strict, pytest --cov. Adapters: 172 tests pass, 96% coverage (+12 new: ConnectionsClient, ManagementApiClient, DecisionsInFlightClient cancel/is_canceled). Backend: 93 tests pass, 92% coverage (+27 new across ws/protocol, ws/connect+disconnect, ws/fanout, ws/default). Deferred per ADR 0019: the <300ms progress-chunk latency criterion, the LocalStack WebSocket round-trip + 10Hz connect/disconnect leak test, and the actual aws-infra Lambda rewiring (see NOTES).
2026-07-31 aws-infra phase-06 (Event stack) done on feat/aws-infra-event, merged into a tmp-main-merge-event branch off origin/main and pushed to main; tagged phase/aws-infra-event-done and checkpoint-4-api-live (closes Wave 4). Merged origin/main (through backend phase-02, sprint step 22) into the branch first -- it had sat at the original baseline commit 3a3e255 since the 43 feat branches were cut. Per ADR 0020: phase-06 §3 lists ~12 EventBridge rules/schedules (GuardDuty/IdC->F5, mgmt-trail-subscription->F6, weekly SLR/F6-report/cost-report, hourly memory-semantic-syncer, 15-min SCP-refresh, 1-min watchdog, KB ingest/corpus-fetch/manifest), every one targeting a specialist Lambda owned by a Wave 6/8 phase that hasn't landed yet -- scoped this phase to the reusable substrate only (`EventStack.register_event_rule()`/`register_schedule()`, mirroring `LambdaStack.new_function()`'s ADR-0011 division of ownership) plus a code-adjacent `PENDING_EVENT_BINDINGS` table naming every deferred rule and its owning phase. Built the 4 composite alarms whose metric/resource already exists on main (SentinelZelkovaViolations, real EMF metric from adapters/zelkova/client.py; SentinelGuardrailInterventions; SessionKillQueue-DLQ depth, real queue from FoundationStack phase-02; SentinelBedrockDollars anomaly-detection band) -- a CloudWatch alarm doesn't need its source metric to already have data, same precedent as phase-08's CrossAccountDriftAlarm. Deliberately did not recreate SentinelBreakGlassAssumption (already real in SecurityStack, phase-01) or the drift-detector schedule (already real in CrossAccountStack, phase-08). Found and fixed a real, pre-existing bug while verifying the toolchain: pytest importing functions/*/handler.py for unit tests writes __pycache__/*.pyc into the same directories every `Code.from_asset()` call zips as Lambda code; since CDK hashes the full directory content for the S3 asset key, this was making every Lambda-asset stack's snapshot non-deterministic depending on test-run order (4-6 stacks failed snapshot-stability on the first full run, in a shifting set each rerun) -- added `LAMBDA_ASSET_EXCLUDES = ["__pycache__", "*.pyc"]` to sentinel_lambda.py and wired it into all 16 `Code.from_asset()` call sites across 10 files, then regenerated all 8 stack snapshots and reverified bit-for-bit stability across two consecutive full pytest runs. cdk synth clean for dev/staging/prod, cdk-nag zero AwsSolutions errors, 119 tests pass (+8 new for EventStack), 99% coverage, ruff/mypy --strict clean. Deferred per ADR 0020: all ~12 PENDING_EVENT_BINDINGS rules (land with their owning phase), the mgmt-trail log-group subscription filter (needs a real org trail ARN), phase-06 §6's "fires within ±60s"/"alarms deliver to SNS" criteria (need a deployed stack on a real AWS dev account), and SentinelGuardrailInterventions/SentinelBedrockDollars EMF emission (adapters' scope, not aws-infra's).
2026-07-31 frontend phase-00 (Frontend Foundations) done on feat/frontend-foundation, merged to main at c3075cd, tagged phase/frontend-foundation-done. Opens Wave 5. Unusually, this branch did NOT sit at the original baseline commit like every prior phase's branch had -- it was already current with main (through aws-infra phase-06, sprint step 23) when picked up, and a substantial chunk of the phase (package.json, tailwind/shadcn setup, the BFF proxy, lib/{auth,session,env,api-client}.ts, mocks/ scaffold, several shadcn components) had already been built uncommitted in a prior session's git worktree. Picked up that WIP rather than redoing it, verified it against phase-00's spec, and built the rest: missing shadcn primitives (Sheet, Toast/Toaster/use-toast, Table -- the spec's full list was Button/Input/Sheet/Toast/Table/Badge/Card/Tabs/Dialog/Skeleton), app/providers.tsx (TanStack Query + next-themes + Toaster), components/auth-gate.tsx + lib/current-session.ts (Server Component session read, split from lib/session.ts's pure cookie codec so that module stays free of next/headers), app/layout.tsx, app/auth/login/page.tsx (mints an OAuth state nonce into a short-lived cookie) + app/auth/callback/route.ts (verifies it, exchanges the code, seals tokens into the signed session cookie), a minimal app/page.tsx dashboard listing the 8 features, vitest.config.ts + a session-codec/env-defaults unit suite, playwright.config.ts + tests/e2e/{auth,axe}.spec.ts, scripts/check-api-drift.mjs, .env.example, frontend-ci.yml, and ADR 0021 (lib/api-types.ts hand-mirrors backend response schemas rather than relying on openapi-typescript codegen, since every backend route returns envelope.ok(model) as a bare dict[str, Any] with nothing for OpenAPI to generate a response type from). The golden auth-flow e2e test exploits lib/env.ts's existing zero-config dev defaults (NEXT_PUBLIC_COGNITO_DOMAIN defaults to "local-dev.auth.invalid") by adding an MSW handler for that exact domain's /oauth2/token endpoint, so the whole login -> callback -> dashboard path is testable with zero real Cognito pool; playwright.config.ts's webServer runs `pnpm dev` (not `build && start`) specifically so mocks/server-bootstrap.ts's MSW gate (`NODE_ENV !== "production"`) stays active for it. Found and fixed four real bugs verifying the toolchain: (1) .eslintrc.json referenced `@typescript-eslint/*` rules without declaring the plugin or parser, so every custom rule was silently unregistered (72 no-op errors surfaced once fixed and re-run correctly); (2) lib/api-client.ts's CSRF-cookie regex `match[1]` typed as possibly `undefined` under tsconfig's `noUncheckedIndexedAccess`; (3) lib/session.ts's HS256 signing key used the environment-global `TextEncoder`, which under Vitest's jsdom environment is a different realm than `jose`'s own module-level `TextEncoder` (jose memoizes one at import time in lib/buffer_utils.js) -- every `sign()` threw `payload must be an instance of Uint8Array`; switched the key to `Buffer.from` (always Node's realm, correct in every real runtime this module executes in) and pinned session.test.ts itself to `@vitest-environment node` since jose's internal encoder is still split from whatever's global; (4) scripts/check-api-drift.mjs invoked `node_modules/.bin/openapi-typescript`, which on Windows is a POSIX shell wrapper pnpm generates alongside a `.cmd` -- `execFileSync("node", [that path])` can't parse it as JS; resolved `node_modules/openapi-typescript/bin/cli.js` directly instead. Real toolchain run: pnpm typecheck/lint/test (5 unit tests)/build/check:api-drift all clean. Two things could not be verified in this dev sandbox and are deferred to CI (see NOTES): the Playwright e2e/axe specs (Chromium download blocked by network policy) and `next build`'s standalone-output symlink step (Windows needs Developer Mode/elevation for unprivileged symlinks; confirmed the actual compile + static generation succeed by temporarily removing `output: "standalone"` and rebuilding).
2026-07-31 frontend phase-01 (Chat Console) done on feat/frontend-chat, merged to main at 1ccf385, tagged phase/frontend-chat-done. This branch was already current with main (unlike most prior branches) since it was picked up directly after phase-00 merged. Built per frontend/docs/phase-01-chat-console.txt: lib/websocket-client.ts (SentinelChatSocket -- reconnects within 1s of an unexpected close, 25s heartbeat ping, parses the SSE-flavored `event: X\ndata: Y\n\n` frames backend/ws/protocol.py's encode_event writes, positionally rather than with an off-the-shelf SSE parser since embedded newlines in a progress chunk aren't re-prefixed per line), store/chat-session.ts (Zustand -- socket instance kept outside reactive state deliberately), components/chat/{chat-console,message-list,progress-line,result-block,finding-chip,citation-inline,prompt-composer,session-rail}.tsx, and a new (dashboard) route group (app/(dashboard)/{layout,page}.tsx + chat/page.tsx) that factors phase-00's AuthGate into one shared layout instead of duplicating it -- app/page.tsx deleted, its content moved to app/(dashboard)/page.tsx (same URL, route groups are invisible in the path). Two real cross-phase gaps were found and fixed while building the first real consumer of aws-infra phase-07's WebSocket API and backend phase-02's fanout service, both documented in ADR 0022: (1) aws-infra's WebSocketLambdaAuthorizer reads `route.request.header.Authorization` on the $connect handshake, which no browser WebSocket can set -- added app/api/ws-token/route.ts (same-origin, reads the session cookie server-side via the same lib/current-session.ts AuthGate uses, returns only the short-lived access token) and moved auth onto the connection URL's `?token=` query string instead; the aws-infra authorizer itself still needs a follow-up change before a live connection can actually succeed, tracked in NOTES + BLOCKERS since fixing infra aws-infra doesn't own is out of scope for a frontend-only phase. (2) backend/ws/fanout.py never echoed the server-minted correlation_id back to the client before a turn completed, so Cancel (`{action: "cancel", correlation_id}` per phase-01 §4) had nothing to send -- added a `started` event emitted once before invoking Prime, and updated the two existing backend/tests/unit/test_ws_fanout.py assertions whose frame-index expectations shifted as a result (re-ran backend's full toolchain: ruff/ruff format/mypy --strict/pytest all clean, 93 tests, 92% coverage). Also found and fixed a frontend toolchain gap: no `@vitejs/plugin-react` was configured for Vitest, so its esbuild JSX transform defaulted to the classic runtime and every component test threw "React is not defined" -- switched vitest.config.ts to `esbuild: { jsx: "automatic" }`. Real toolchain run: pnpm typecheck/lint/test (14 unit tests, +9 over phase-00)/check:api-drift all clean; `next build`'s compile + static generation confirmed clean the same way as phase-00 (temporarily removing `output: "standalone"` to work around the same Windows symlink-permission gap). Deferred to CI per NOTES: the Playwright chat-flow spec (uses `page.routeWebSocket` to mock the server side of the real client) and the chat-page axe scan -- same Chromium-download sandbox limit as phase-00.
